const os = require("os");
// console.log(os.arch());  to give the bits value
// freemem() //to give the ream storage in the form of butes
// console.log(os.freemem() / (1024 * 1024 * 1024));
// hostname() to chheck the hostname of the pc
// console.log(os.totalmem() / (1024 * 1024 * 1024));
// console.log(os.hostname());
// console.log(os.platform());
console.log(os.userInfo());
