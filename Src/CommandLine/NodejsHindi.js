// Make a file in a ndoejs
// file name print in comsole
// create a file
// file--->create,list ,delete,upadte
const fs = require("fs");
// import the module of filesystem
const path = require("path");
const dirPath = path.join(__dirname, "Files");
console.log(dirPath)
// for (i = 0; i < 5; i++)00000000000000 {
//   fs.writeFileSync(dirPath + "/hello"+i + ".txt", "a simple multi file crateing");
// }
